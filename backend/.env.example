# Nana's Bites — production environment template
# Copy to .env and set values. Never commit .env.

# Admin user for dashboard (run: python manage.py create_admin_user)
 ADMIN_EMAIL=admin@cheffarah.com
 ADMIN_PASSWORD=farahammar123!
 ADMIN_FULL_NAME=Admin Farah Ammar

# Django
DJANGO_DEBUG=1
DJANGO_SECRET_KEY=django-insecure-7!x1$k2l9z#b8q3w5m0p2v6r1u8y4s0e6f!a2t31
ALLOWED_HOSTS=localhost,127.0.0.1,cheffarahammar.com,www.cheffarahammar.com,api.cheffarahammar.com

# Database (PostgreSQL)
DB_NAME=nanas_bites
DB_USER=nanas_bites_user
DB_PASSWORD=TestDBPass1230
DB_HOST=127.0.0.1
DB_PORT=5432

# CORS (frontend origin)
CORS_ORIGINS=https://cheffarahammar.com,https://www.cheffarahammar.com

# Security (production behind HTTPS proxy)
SECURE_SSL_REDIRECT=1

# Stripe
STRIPE_SECRET_KEY=sk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Checkout (optional)
CHECKOUT_TAX_RATE=0.17
CHECKOUT_SHIPPING_FIXED=15

# JWT
JWT_ACCESS_MINUTES=60
JWT_REFRESH_DAYS=7

# Throttling
THROTTLE_ANON=100/hour
THROTTLE_USER=200/hour
THROTTLE_AUTH=10/hour

# Login lockout (high security)
LOGIN_FAILED_MAX_ATTEMPTS=5
LOGIN_LOCKOUT_DURATION=900

# Session (seconds; default 2 hours)
SESSION_COOKIE_AGE=7200

# Security headers (enable HSTS when on HTTPS, e.g. 31536000 for 1 year)
# SECURE_HSTS_SECONDS=31536000

# Logging
LOG_LEVEL=INFO

# Sentry (optional; leave empty to disable)
SENTRY_DSN=

# Email (for ADMINS error notifications)
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_USE_TLS=1
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
SERVER_EMAIL=noreply@yourdomain.com
DEFAULT_FROM_EMAIL=noreply@yourdomain.com
ADMINS=Admin Name,admin@yourdomain.com
